# lien-review-testbed

Tracked multi-language sample app (PHP, Python, Rust, TypeScript) kept as
fixture material for `packages/review/test/harness/`. Not a real product —
plant a synthetic regression here and capture it as a harness fixture when no
suitable real closed PR exists (see the harness README's "Workflow: adding a
new rule").

Do not clean this up or treat it as dead code.
