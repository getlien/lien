# Re-derived by scripts/fetch-vendor.mjs -- not committed (23MB, mostly the
# generated src/parser.c). See that script for the sha256-pinned download +
# patch pipeline.
vendor/

# Rust build output.
target/

# Local dev build produced by `npm run build:native`, and any platform
# package's compiled binary picked up during testing.
*.node

# Generated by scripts/publish-platform-packages.mjs at CI publish/pack time
# (release.yml, ci.yml's release-smoke-test) -- never hand-edited or
# committed, see that script's header.
artifacts/
platform-packages/
*.tgz
