{
  "name": "@liendev/action",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Action for AI-assisted code review using Lien complexity analysis",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --source-map --license licenses.txt --external @lancedb/lancedb --external tree-sitter --external tree-sitter-javascript --external tree-sitter-typescript --external tree-sitter-python --external tree-sitter-php --external @xenova/transformers && echo '{\"type\":\"commonjs\"}' > dist/package.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0",
    "@liendev/core": "*",
    "collect.js": "^4.36.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vercel/ncc": "^0.38.4",
    "typescript": "^5.3.0",
    "vitest": "^4.0.8"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
